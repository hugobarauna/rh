require 'spec_helper'

describe Benefit do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Benefit.create!(@valid_attributes)
  end
end
