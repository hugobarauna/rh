<h1>Listando Cargos</h1>

<table>
  <tr>
		<th>Nome</th>
  </tr>

	<% @roles.each do |role| %>
	  <tr>
			<td><%=h role.name %></td>
	    <td><%= link_to 'Ver', role %></td>
	    <td><%= link_to 'Editar', edit_role_path(role) %></td>
	    <td><%= link_to 'Deletar', role, :confirm => 'Você tem certeza?', :method => :delete %></td>
	  </tr>
	<% end %>

</table>

<br />

<%= link_to 'Novo Cargo', new_role_path %>